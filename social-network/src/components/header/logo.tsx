import React from "react";
import {Image} from 'semantic-ui-react'
import logo from '../../images/logo2.jpg'

const Logo = () => {
    return (
        <Image src={logo}/>
    );
};

export default Logo;
