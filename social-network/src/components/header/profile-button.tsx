import React from "react";

const ProfileButton = () => {
    return (
        <a href="/profile"><button 
            className="ui primary button"
            >Profile
        </button></a>
    );
};

export default ProfileButton;
